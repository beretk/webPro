20230822

<JacaScript>

*반복문
-배열, 배열에서의 반복문 과 forEach()함수, do~while문, while문
1. 배열
-어떤 자료형도 배열 요소가 될수 있다.
:자료의 집합, 배열의 크기가 가변적
-forEach(반복), push(배열추가), pop(배열삭제), slice vs splice(배열 추출)
:slice는 배열의 원본은 바꾸지 않고 새로운 배열 반환
:splice(n, m); // n번째 인덱스부터 m개 제거

*함수
1.익명함수
-익명함수 호출은 변수 할당 후 사용가능
-익명함수 호출은 변수의 이름으로 호출
2.선언적함수
-선언적 함수는 위에서도(변수 할당 전) 사용가능
-함수의 이름으로 호출
-선언적함수의 재정의:아래에서 정의된 함수가 출력
3.매개변수와 리턴값
-선언된 매개변수 보다 많은 경우 뒷부분 무시
-선언된 매개변수 보다 적은 경우 , 없는 나머지는 undefined.
-return result; return없으면 undefined
4.가변인자함수  
-Array:매개변수의 갯수에 따라 변하는 함수. 화살표 함수에서는 불가
-array
:매개변수 0개일 때 ->length가 0인 배열을 return
:매개변수 1개일 때 -> length가 매개변수만큼의 크기를 가지는 배열 return
:매개변수 2개 이상일 때 -> 매개변수로 배열을 생성하여 return

